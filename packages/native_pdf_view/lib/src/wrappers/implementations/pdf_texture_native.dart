import 'package:flutter/widgets.dart';

// ignore: non_constant_identifier_names
// Texture PdfTexture({
//   required int textureId,
//   Key? key,
// }) =>
//     Texture(key: key, textureId: textureId);

typedef PdfTexture = Texture;
